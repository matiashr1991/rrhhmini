# Para desarrollo local (apuntar al backend NestJS directo)
NEXT_PUBLIC_API_URL=http://localhost:8000

# En producción NO seteás esta variable — se usa /api por defecto
# y Traefik rutea /api → backend:8000
